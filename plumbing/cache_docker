#!/bin/bash

set -o errexit
set -o nounset
set -o xtrace

FILE="vendor/docker/python.tar"

if [[ -e ${FILE} ]]; then
  docker load -i ${FILE}
else
  docker pull python:2.7
  docker save python:2.7 > ${FILE}
fi

