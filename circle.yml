machine:
  services:
    - docker

dependencies:
  cache_directories:
    - "vendor"
  override:
    - ./plumbing/cache_docker metrics
    - ./script/bootstrap

test:
  override:
    - ./script/feature
